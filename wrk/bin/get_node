R=$(readlink -m $(dirname $0)/..)
cd $R

if [ `perl -e "exit 1 unless 'init'=~m/ ?init ?/"` ]; then
  sudo apt-get install -y libssl-dev libltdl-dev
else
  echo xxx
fi

exit 0

T=node
if [ -d $T/ ]; then
  cd $T
  git reset --hard
  git pul
else
  git clone git://github.com/joyent/node.git
  cd $T/
fi


./configure --prefix=$HOME/opt
make
make install
